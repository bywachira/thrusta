export interface IUseSelector {
    app: {
        nodes: any[],
        node: Object,
        processes: any[],
        process: Object,
        commands: any[],
        command: Object,
        isLoading: boolean;
        error: string;
    }
}